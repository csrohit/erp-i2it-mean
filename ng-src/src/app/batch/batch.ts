export interface Batch {
    _id?: string;
    title: string;
    department?: string;
    subjects?: string[];
    room?: string;
    strength?: number;
    head?: string;
}
// ? stay up to date with `/models/batch.js`
